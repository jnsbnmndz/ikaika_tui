# Technology stack
- Python >=3.11; setuptools build backend.
- Package: `company-tui`; console entry point `company = company_tui.cli:main`.
- Sole runtime dependency: Textual >=0.60.
- Tests use standard-library `unittest`.